import React from 'react'

const ChangePassword = () => {
  return (
    <div><h1> if (!isNameChanged && !isPhotoChanged) {
    toast.info("No changes were made");
    setIsEditing(false);
    return;
  }</h1></div>
  )
}

export default ChangePassword